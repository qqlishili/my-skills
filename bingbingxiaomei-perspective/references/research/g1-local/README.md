# G1 纯本地全量重读证据

本目录只保存 2026-07-19 语义重构 G1 的临时证据产物。

- 输入仅限 `references/sources/articles/` 下的 520 篇 Markdown 原文。
- 本目录不代表当前 taxonomy，不覆盖既有 `references/research/01-07`，也不修改 `SKILL.md`、模型文档或分类器。
- G2 用户审查前，此处的候选结论均为待确认结论。
