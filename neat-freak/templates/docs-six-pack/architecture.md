# 架构与核心流程

## 一句话

<!-- Bridge 是不是 orchestrator? 谁选 worker? 一句话定位。 -->

## 端到端流程（输入 → 处理 → 判断 → 输出 → 验证/反馈）

```
[输入]
   ↓
[处理]
   ↓
[判断]
   ↓
[输出]
   ↓
[验证/反馈]
```

## 关键不变量

<!-- 哪些规则绝对不能违反 -->

## 已知不变量例外（要改前必读）

<!-- 不改会出错、但很多人会直觉想改的 -->