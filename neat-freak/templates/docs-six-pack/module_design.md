# 模块接口设计

> 用途：明确各模块的**对外契约**（import 谁、暴露什么、被谁调用）。改动前先读这里确认边界。

## 模块依赖图

```
server.py
  ├─→ ...
  └─→ ...
```

**死代码嫌疑**（import 了 ... 但主路径没调用）：

- `<file>` — ...

## 公开接口契约

### MCP tool 表面 / CLI 表面 / ...

| Tool | 输入 | 输出 | 副作用 |
|---|---|---|---|
|  |  |  |  |

### Handle / Spec / Rule 契约

```python
@dataclass
class X:
    field: type
    ...
```

### 全局不变量

- ...

## 改动接口前必读

1. ...